## Friction

None. The dispatch card was fully self-contained: the exact import/element to remove was
pre-confirmed by grep, the shared surface was already shipped and matched the brief, and the
live-verification steps mapped 1:1 onto the acceptance table. No escalation needed — this was a
pure `src/views/**` deletion task with no `src/components/**` edits at all.

## Proposed guide updates

None.
